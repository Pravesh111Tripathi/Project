package Interface.markerInterface;

public interface deletable {

    /*
      it is an interface which does not contain anything inside
      it is just used to mark the class which are having some special features
      we can also make our own marker interface
     */
}
